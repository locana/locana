<UserControl
    x:Class="Kazyx.Uwpmm.Control.SettingSection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kazyx.Uwpmm.Control"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="LayoutRoot" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,10,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Border Background="{StaticResource AppBarItemDisabledForegroundThemeBrush}" Margin="0,5,0,10" >
            <TextBlock Name="TitleTextBlock" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                       FontSize="24" Foreground="{ThemeResource AccentColorBrush}" Text="" Margin="8,5,0,5" />
        </Border>
        <StackPanel Name="SettingItems" Grid.Row="1" Margin="16,0" />
    </Grid>
</UserControl>
